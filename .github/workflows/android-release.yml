name: Android Release

on:
  push:
    tags:
      - "v*-b*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    env:
      CI: 1
      EXPO_PUBLIC_SUPABASE_URL: ${{ secrets.EXPO_PUBLIC_SUPABASE_URL }}
      EXPO_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.EXPO_PUBLIC_SUPABASE_ANON_KEY }}
      EXPO_PUBLIC_APP_UPDATE_GITHUB_REPO: ${{ secrets.EXPO_PUBLIC_APP_UPDATE_GITHUB_REPO }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Install dependencies
        run: npm ci

      - name: Validate release env
        run: |
          test -n "${EXPO_PUBLIC_SUPABASE_URL}" || { echo "Missing secret: EXPO_PUBLIC_SUPABASE_URL"; exit 1; }
          test -n "${EXPO_PUBLIC_SUPABASE_ANON_KEY}" || { echo "Missing secret: EXPO_PUBLIC_SUPABASE_ANON_KEY"; exit 1; }

      - name: Prepare release version
        run: node scripts/prepare-android-release.mjs "${GITHUB_REF_NAME}"

      - name: Generate Android project
        run: npx expo prebuild --platform android --no-install --clean

      - name: Configure Android SDK path
        run: echo "sdk.dir=${ANDROID_SDK_ROOT}" > android/local.properties

      - name: Build release APK
        working-directory: android
        run: ./gradlew assembleRelease

      - name: Rename APK
        run: cp android/app/build/outputs/apk/release/app-release.apk "nossa-clima-${GITHUB_REF_NAME}.apk"

      - name: Publish GitHub release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${GITHUB_REF_NAME}" "nossa-clima-${GITHUB_REF_NAME}.apk" \
            --title "Nossa Clima ${GITHUB_REF_NAME}" \
            --generate-notes
