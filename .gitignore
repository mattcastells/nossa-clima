node_modules
.expo
.env
npm-debug.log*
coverage
.DS_Store
android

.yarn/
yarn.lock
.yarnrc.yml

# Las skills de .claude/skills/ si se versionan; los permisos locales no.
.claude/settings.local.json

# Estado local de la CLI de Supabase (link, versiones). No se versiona.
supabase/.temp/

# Salida del relevamiento de proveedores: SQL generado, reportes y cache HTTP.
# El SQL se revisa y se corre, no se versiona. Las semillas y las consultas si.
artifacts/
tools/supplier-survey/.cache/
